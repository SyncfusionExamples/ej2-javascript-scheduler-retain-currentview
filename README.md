# JavaScript Scheduler with Custom View Intervals and View Persistence

This project is a skeleton quick-start application built with Essential JS 2, demonstrating how to use the [JavaScript Scheduler](https://www.syncfusion.com/javascript-ui-controls/js-scheduler) for managing events, appointments, and schedules. It provides a preconfigured setup with common settings, enabling developers to quickly build modern web applications with a user-friendly scheduling interface.

## Features

- Pre-configured Scheduler component
- Syncfusion Essential JS 2 integration
- Browser-sync support for live reloading
- SystemJS module loading
- Gulp build automation
- Ready-to-use project structure
- All common settings preconfigured

## Prerequisites

Before you begin, ensure you have the following installed:
- Node.js (version 12 or higher)
- npm (Node Package Manager)
- Gulp CLI (optional, for build automation)

## Installing

We can get all the Essential JS 2 components in a single npm package [`ej2`](https://www.npmjs.com/package/@syncfusion/ej2).

We already configure the required packages in the `package.json` file.

You can run the below command to install all dependent packages related to this seed project.

```
npm install
```

## Running

The application is configured with `browser-sync`, so it will serve the web application in your default browser.

We used `SystemJS` for module loading.

You can use the below npm script to run the web application.

```
gulp serve
```

## Documentation

For comprehensive guidance on using Essential JS 2 components, visit the official documentation:

- **Introduction:** https://ej2.syncfusion.com/javascript/documentation/introduction
- **Scheduler Component:** https://ej2.syncfusion.com/javascript/documentation/schedule/getting-started

## Support

If you have any questions or need assistance, visit the [Syncfusion support portal](https://support.syncfusion.com) or explore the [Syncfusion community forums](https://www.syncfusion.com/forums).